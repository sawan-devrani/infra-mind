apiVersion: v1
kind: Service
metadata:
  name: infra-mind-svc
  # namespace: infra-mind
  namespace: default
spec:
  selector:
    app: infra-mind
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
